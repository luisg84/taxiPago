import 'package:flutter/material.dart';

class antojitosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('antojitos'),
      ),
    );
  }
}
