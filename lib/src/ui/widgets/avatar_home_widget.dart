import 'package:flutter/material.dart';


class AvatarHomeWidget extends StatelessWidget {
  const AvatarHomeWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return avatar
  }
}